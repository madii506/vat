# vat

10,000 simulated apes in a vat, evolving against the real January 2021 GameStop tape. Paired to $GME on Robinhood Chain.

Everything runs in the browser. `assets/tape.json` is the data; `index.html` is the whole site.
